# Mobile application placeholder

The UI application is intentionally not scaffolded in this seed repository. A coding agent should implement it according to `AGENTS.md` and `docs/11-codex-build-plan.md`.

Recommended first command at implementation time should use the current stable official Expo template, not a version copied blindly from this document. Record selected versions in `docs/decisions/003-cross-platform-stack.md`.

The first deliverable is only the complete `same-picture` vertical slice.
